<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rescue Edit Page</title>
</head>
<body>
    <form action="/rescue/<%= animal.id%>?_method=PUT" method="POST">
        <label for="name">name</label>
        <input value="<%= animal.name %>" name="name" type="text">
        <label for="sex">sex</label>
        <input value="<%= animal.sex %>" name="sex" type="text">
        <label for="size">size</label>
        <input value="<%= animal.size %>" name="size" type="text">
        <label for="color">color</label>
        <input value="<%= animal.color %>" name="color" type="text">
        <label for="breed">breed</label>
        <input value="<%= animal.breed %>" name="breed" type="text">
        <label for="lifeStage">life stage</label>
        <input value="<%= animal.lifeStage %>" name="lifeStage" type="text">
        <label for="img">img</label>
        <input value="<%= animal.img %>" name="img">
        <input type="submit" value="Edit">
    </form>
</body>
</html>