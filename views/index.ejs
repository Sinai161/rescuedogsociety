<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Index Rescue Page</title>
</head>
<body> 
<ul>
    <h1>Rescue Dog Society</h1>
    <% for(let i = 0; i < animals.length; i++) { %>
    <li>
        <a href="/rescue/<%=animals[i].id%>">
            <img src="<%= animals[i].img%>" >
            <%= animals[i].name %>
        </a> 
        <a href="/rescue/<%= animals[i].id %>/edit" type="button">Edit</a>
    </li>
    <% } %> 
</ul>
</body>
</html>